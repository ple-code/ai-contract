# 虚拟环境
.venv/
venv/

# 大文件：原始音频不入库（太大，且可从原来源恢复）
raw/*.m4a
raw/*.mp3
raw/*.wav
raw/*.aac
raw/*.flac
raw/*.mp4
raw/*.mov

# 中间产物
tmp/

# 模型缓存
.cache/

# 系统文件
.DS_Store
*.swp
*~

# Obsidian 配置（按需保留，先忽略）
.obsidian/workspace*
